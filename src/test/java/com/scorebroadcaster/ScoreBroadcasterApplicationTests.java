package com.scorebroadcaster;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class ScoreBroadcasterApplicationTests {

    @Test
    void contextLoads() {
        // Verifies application context starts (requires MySQL + RabbitMQ when not mocked)
    }
}
